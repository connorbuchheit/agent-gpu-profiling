# LLM client (OpenAI-compatible; works with vLLM and SGLang)
openai>=1.0.0

# GPU metrics
pynvml>=11.0.0

# Config and CLI
pyyaml>=6.0
python-dotenv>=1.0.0

# Inference matrix benchmark (inference_profiler)
aiohttp>=3.9.0
numpy>=1.24.0
pandas>=2.0.0
tabulate>=0.9.0
plotly>=5.18.0
psutil>=5.9.0

# Optional: vLLM / torch (install separately for server + Chrome traces)
# vllm>=0.3.0
# torch>=2.1.0

# Optional: terminal graph (pip install plotext)
# plotext>=5.0.0
