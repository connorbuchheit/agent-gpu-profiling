# LLM client (OpenAI-compatible; works with vLLM and SGLang)
openai>=1.0.0

# GPU metrics
pynvml>=11.0.0

# Config and CLI
pyyaml>=6.0
python-dotenv>=1.0.0

# Optional: terminal graph (pip install plotext)
# plotext>=5.0.0
